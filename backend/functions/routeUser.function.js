const googleClient = require('@googlemaps/google-maps-services-js').Client;
const client = new googleClient({});

const routeUser =  (freetime, source, destination, legs, googleMapsKey) => {

}

module.exports = {routeUser}